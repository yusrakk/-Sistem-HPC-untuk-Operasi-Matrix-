# 🏗️ System Architecture - HPC Matrix Operations (4096×4096)

## 📐 Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE LAYER                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐ │
│  │   Makefile   │  │  CLI Scripts │  │ Python API   │  │ Config File │ │
│  │  Automation  │  │   Commands   │  │   Interface  │  │   (*.conf)  │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    APPLICATION LAYER (Python/C++)                        │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │              Matrix Operations Core                              │   │
│  │  ┌─────────────────┐         ┌─────────────────┐               │   │
│  │  │ C++ MPI+OpenMP  │         │ Python mpi4py   │               │   │
│  │  │   Implementation │         │  Implementation │               │   │
│  │  │                 │         │                 │               │   │
│  │  │ • Matrix Mult   │         │ • Matrix Mult   │               │   │
│  │  │ • Matrix Inv    │         │ • NumPy Opt     │               │   │
│  │  │ • Hybrid Parallel│        │ • MPI Parallel  │               │   │
│  │  └─────────────────┘         └─────────────────┘               │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │              Supporting Services                                 │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │   │
│  │  │ Distributed │  │  Resource   │  │  Analysis & Reporting   │ │   │
│  │  │   Storage   │  │  Monitor    │  │  • Strong Scaling       │ │   │
│  │  │   Manager   │  │             │  │  • Weak Scaling         │ │   │
│  │  │             │  │ • CPU       │  │  • Bottleneck Analysis  │ │   │
│  │  │ • HDF5      │  │ • Memory    │  │  • Visualization        │ │   │
│  │  │ • Chunking  │  │ • Network   │  │                         │ │   │
│  │  │ • Compress  │  │ • Disk I/O  │  │                         │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    PARALLELIZATION LAYER                                 │
│  ┌────────────────────────────────────────────────────────────────┐     │
│  │                    MPI Layer (OpenMPI)                         │     │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │     │
│  │  │ Process  │  │ Process  │  │ Process  │  │ Process  │     │     │
│  │  │  Rank 0  │  │  Rank 1  │  │  Rank 2  │  │  Rank N  │ ... │     │
│  │  │  (Root)  │  │          │  │          │  │          │     │     │
│  │  └─────┬────┘  └─────┬────┘  └─────┬────┘  └─────┬────┘     │     │
│  │        │             │             │             │           │     │
│  │        └─────────────┴─────────────┴─────────────┘           │     │
│  │                          │                                    │     │
│  │              ┌───────────┴───────────┐                       │     │
│  │              ▼                       ▼                       │     │
│  │      ┌──────────────┐        ┌──────────────┐              │     │
│  │      │   Broadcast  │        │ Scatter/     │              │     │
│  │      │   (MPI_Bcast)│        │  Gather      │              │     │
│  │      └──────────────┘        └──────────────┘              │     │
│  └────────────────────────────────────────────────────────────────┘     │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────┐     │
│  │              OpenMP Layer (Shared Memory)                      │     │
│  │                                                                │     │
│  │  Per-Process Thread Pool:                                     │     │
│  │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐                     │     │
│  │  │Thread│  │Thread│  │Thread│  │Thread│  ...                 │     │
│  │  │  0   │  │  1   │  │  2   │  │  N   │                     │     │
│  │  └──────┘  └──────┘  └──────┘  └──────┘                     │     │
│  │     │         │         │         │                          │     │
│  │     └─────────┴─────────┴─────────┘                          │     │
│  │               │                                               │     │
│  │     #pragma omp parallel for                                 │     │
│  │               │                                               │     │
│  │     ┌─────────▼─────────┐                                    │     │
│  │     │  Loop Iterations  │                                    │     │
│  │     │   (Matrix Rows)   │                                    │     │
│  │     └───────────────────┘                                    │     │
│  └────────────────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        STORAGE LAYER                                     │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                 Distributed File System                         │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌────────────────┐ │   │
│  │  │  HDF5 Files     │  │   CSV Logs      │  │  JSON Results  │ │   │
│  │  │                 │  │                 │  │                │ │   │
│  │  │ • matrix_A.h5   │  │ • performance   │  │ • scaling_data │ │   │
│  │  │ • matrix_B.h5   │  │ • bottleneck    │  │ • metadata     │ │   │
│  │  │ • matrix_C.h5   │  │ • monitoring    │  │                │ │   │
│  │  │                 │  │                 │  │                │ │   │
│  │  │ Compressed      │  │ Time-series     │  │ Structured     │ │   │
│  │  │ Chunked         │  │ Data            │  │ Analysis       │ │   │
│  │  └─────────────────┘  └─────────────────┘  └────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    VISUALIZATION & OUTPUT LAYER                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Matplotlib/Seaborn Visualization Engine                        │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌────────────────┐ │   │
│  │  │ Strong Scaling  │  │   Bottleneck    │  │   Resource     │ │   │
│  │  │     Plots       │  │    Analysis     │  │   Monitoring   │ │   │
│  │  │                 │  │                 │  │                │ │   │
│  │  │ • Time vs Proc  │  │ • Comp vs Comm  │  │ • CPU Usage    │ │   │
│  │  │ • Speedup       │  │ • Overhead %    │  │ • Memory       │ │   │
│  │  │ • Efficiency    │  │ • Load Balance  │  │ • Network I/O  │ │   │
│  │  └─────────────────┘  └─────────────────┘  └────────────────┘ │   │
│  │                                                                 │   │
│  │  Output: PNG (300 DPI), PDF, SVG                               │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Architecture

### 1️⃣ Matrix Multiplication Workflow

```
┌─────────────────────────────────────────────────────────────────────┐
│                   MATRIX MULTIPLICATION FLOW                         │
└─────────────────────────────────────────────────────────────────────┘

Step 1: Initialization
─────────────────────────────────────────────────────────────────────
   ┌──────────────────┐
   │  Root Process    │
   │   (Rank 0)       │
   │                  │
   │  Generate/Load:  │
   │  • Matrix A      │
   │  • Matrix B      │
   │  (4096×4096)     │
   └────────┬─────────┘
            │
            ▼

Step 2: Data Distribution (MPI)
─────────────────────────────────────────────────────────────────────
   ┌──────────────────────────────────────────────────────────────┐
   │              MPI_Bcast (Matrix B to all)                     │
   │  Root sends full Matrix B to all processes                   │
   └───────────────────────────┬──────────────────────────────────┘
                               │
   ┌───────────────────────────┴──────────────────────────────────┐
   │            MPI_Scatter (Matrix A row-wise)                   │
   │  Root distributes rows of Matrix A to processes              │
   └───────────────────────────┬──────────────────────────────────┘
                               │
            ┌──────────────────┼──────────────────┐
            ▼                  ▼                  ▼
   ┌────────────────┐ ┌────────────────┐ ┌────────────────┐
   │  Process 0     │ │  Process 1     │ │  Process N     │
   │                │ │                │ │                │
   │  Rows: 0-1023  │ │  Rows: 1024-   │ │  Rows: ...     │
   │                │ │       2047     │ │                │
   │  Full Matrix B │ │  Full Matrix B │ │  Full Matrix B │
   └────────┬───────┘ └────────┬───────┘ └────────┬───────┘
            │                  │                  │
            ▼                  ▼                  ▼

Step 3: Local Computation (MPI + OpenMP Hybrid)
─────────────────────────────────────────────────────────────────────
   ┌──────────────────────────────────────────────────────────────┐
   │         Each Process (MPI Parallelism)                       │
   │                                                              │
   │   ┌────────────────────────────────────────────────┐        │
   │   │  OpenMP Thread Pool (Shared Memory)            │        │
   │   │                                                │        │
   │   │  #pragma omp parallel for                      │        │
   │   │  for (row in my_rows) {                        │        │
   │   │    Thread 0: row[0, 4, 8,  ...]               │        │
   │   │    Thread 1: row[1, 5, 9,  ...]               │        │
   │   │    Thread 2: row[2, 6, 10, ...]               │        │
   │   │    Thread 3: row[3, 7, 11, ...]               │        │
   │   │                                                │        │
   │   │    for (col in 0..4095) {                      │        │
   │   │      C[row][col] = dot(A[row], B[:][col])     │        │
   │   │    }                                           │        │
   │   │  }                                             │        │
   │   └────────────────────────────────────────────────┘        │
   │                                                              │
   │   Result: Partial Matrix C (my_rows × 4096)                 │
   └──────────────────────────────────────────────────────────────┘

Step 4: Result Gathering (MPI)
─────────────────────────────────────────────────────────────────────
   ┌────────────────┐ ┌────────────────┐ ┌────────────────┐
   │  Process 0     │ │  Process 1     │ │  Process N     │
   │  Partial C     │ │  Partial C     │ │  Partial C     │
   └────────┬───────┘ └────────┬───────┘ └────────┬───────┘
            │                  │                  │
            └──────────────────┼──────────────────┘
                               │
                    ┌──────────▼───────────┐
                    │    MPI_Gather        │
                    │  (Collect all parts) │
                    └──────────┬───────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │   Root Process       │
                    │  Full Matrix C       │
                    │   (4096×4096)        │
                    └──────────┬───────────┘
                               │
                               ▼

Step 5: Storage (Distributed HDF5)
─────────────────────────────────────────────────────────────────────
   ┌──────────────────────────────────────────────────────────────┐
   │         Distributed Storage Manager                          │
   │                                                              │
   │   Matrix C → HDF5 File                                      │
   │   • Chunking: 1024×1024 blocks                              │
   │   • Compression: gzip level 4                               │
   │   • Checksum: SHA256                                        │
   │   • Metadata: timestamp, size, processor_count              │
   │                                                              │
   │   Output: matrix_C_distributed_4096x4096.h5 (128 MB)        │
   └──────────────────────────────────────────────────────────────┘
```

---

## 🧩 Component Architecture

### 1. Matrix Operations Layer

```
┌─────────────────────────────────────────────────────────────────┐
│                MATRIX OPERATIONS COMPONENTS                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  C++ Implementation (matrix_operations_mpi.cpp)          │  │
│  │  ────────────────────────────────────────────────────    │  │
│  │                                                          │  │
│  │  Classes/Functions:                                      │  │
│  │  • matrix_multiply_mpi()                                 │  │
│  │    - MPI_Comm_rank(), MPI_Comm_size()                    │  │
│  │    - MPI_Bcast() for Matrix B                            │  │
│  │    - MPI_Scatter() for Matrix A rows                     │  │
│  │    - #pragma omp parallel for (OpenMP)                   │  │
│  │    - MPI_Gather() for results                            │  │
│  │                                                          │  │
│  │  • matrix_inverse_mpi()                                  │  │
│  │    - Gauss-Jordan elimination                            │  │
│  │    - Distributed pivoting                                │  │
│  │    - Row operations with MPI                             │  │
│  │                                                          │  │
│  │  • calculate_bottleneck()                                │  │
│  │    - Computation time tracking                           │  │
│  │    - Communication time tracking                         │  │
│  │    - Load imbalance detection                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Python Implementation (matrix_operations_python.py)     │  │
│  │  ──────────────────────────────────────────────────────  │  │
│  │                                                          │  │
│  │  Classes/Functions:                                      │  │
│  │  • matrix_multiply_mpi()                                 │  │
│  │    - comm.Get_rank(), comm.Get_size()                    │  │
│  │    - comm.Bcast() for Matrix B                           │  │
│  │    - comm.Scatter() for Matrix A rows                    │  │
│  │    - np.dot() (NumPy BLAS optimization)                  │  │
│  │    - comm.Gather() for results                           │  │
│  │                                                          │  │
│  │  • matrix_inversion()                                    │  │
│  │    - np.linalg.inv() with error handling                 │  │
│  │                                                          │  │
│  │  • analyze_bottleneck()                                  │  │
│  │    - Time tracking with time.time()                      │  │
│  │    - Communication overhead calculation                  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 2. Distributed Storage Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│              DISTRIBUTED STORAGE SYSTEM                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  DistributedStorageManager Class                         │  │
│  │  (distributed_storage.py)                                │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │                                                          │  │
│  │  Methods:                                                │  │
│  │  • save_matrix_distributed()                             │  │
│  │    ├─ Chunk matrix into blocks                           │  │
│  │    ├─ Compress with gzip                                 │  │
│  │    ├─ Generate checksum (SHA256)                         │  │
│  │    ├─ Write to HDF5 format                               │  │
│  │    └─ Store metadata (JSON)                              │  │
│  │                                                          │  │
│  │  • load_matrix_distributed()                             │  │
│  │    ├─ Read from HDF5                                     │  │
│  │    ├─ Verify checksum                                    │  │
│  │    ├─ Decompress chunks                                  │  │
│  │    └─ Reconstruct full matrix                            │  │
│  │                                                          │  │
│  │  • get_storage_info()                                    │  │
│  │    └─ Return metadata, size, compression ratio           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  Storage Format:                                                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  HDF5 File Structure:                                   │   │
│  │  ├─ /matrix (4096×4096 dataset)                         │   │
│  │  │  ├─ chunks: (1024, 1024)                             │   │
│  │  │  ├─ compression: gzip, level 4                       │   │
│  │  │  └─ dtype: float64                                   │   │
│  │  ├─ /metadata (attributes)                              │   │
│  │  │  ├─ shape: (4096, 4096)                              │   │
│  │  │  ├─ timestamp: ISO 8601                              │   │
│  │  │  ├─ checksum: SHA256                                 │   │
│  │  │  ├─ processor_count: int                             │   │
│  │  │  └─ compression_ratio: float                         │   │
│  │  └─ File size: ~128 MB (compressed from 134 MB)         │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 3. Resource Monitoring Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│              RESOURCE MONITORING SYSTEM                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  ResourceMonitor Class (resource_monitor.py)             │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │                                                          │  │
│  │  Monitoring Components:                                  │  │
│  │                                                          │  │
│  │  1. CPU Monitoring                                       │  │
│  │     ├─ psutil.cpu_percent(interval=0.5)                  │  │
│  │     ├─ Per-core usage                                    │  │
│  │     └─ Average utilization                               │  │
│  │                                                          │  │
│  │  2. Memory Monitoring                                    │  │
│  │     ├─ psutil.virtual_memory()                           │  │
│  │     ├─ Total, available, used                            │  │
│  │     ├─ Percentage utilization                            │  │
│  │     └─ Swap memory status                                │  │
│  │                                                          │  │
│  │  3. Network I/O Monitoring                               │  │
│  │     ├─ psutil.net_io_counters()                          │  │
│  │     ├─ Bytes sent/received                               │  │
│  │     ├─ Packets sent/received                             │  │
│  │     └─ Transfer rate calculation                         │  │
│  │                                                          │  │
│  │  4. Disk I/O Monitoring                                  │  │
│  │     ├─ psutil.disk_io_counters()                         │  │
│  │     ├─ Read/write bytes                                  │  │
│  │     ├─ Read/write operations                             │  │
│  │     └─ I/O rate calculation                              │  │
│  │                                                          │  │
│  │  Methods:                                                │  │
│  │  • start_monitoring()                                    │  │
│  │    └─ Start background thread                            │  │
│  │  • stop_monitoring()                                     │  │
│  │    └─ Stop thread and save data                          │  │
│  │  • get_current_stats()                                   │  │
│  │    └─ Return current resource usage                      │  │
│  │  • export_to_csv()                                       │  │
│  │    └─ Save time-series data                              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  Output Format (CSV):                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  timestamp,cpu_percent,memory_used,network_sent,        │   │
│  │            disk_read,disk_write                         │   │
│  │  2025-12-03 10:39:24,85.2,4096,1024000,512000,256000    │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 4. Scaling Analysis Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│              SCALING ANALYSIS SYSTEM                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Strong Scaling Analysis (strong_scaling.py)             │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │                                                          │  │
│  │  Algorithm:                                              │  │
│  │  1. For each processor count [1, 2, 4, 8]:              │  │
│  │     ├─ Run matrix operations with MPI                    │  │
│  │     ├─ Measure execution time                            │  │
│  │     └─ Record results                                    │  │
│  │                                                          │  │
│  │  2. Calculate metrics:                                   │  │
│  │     ├─ Speedup = T(1) / T(N)                             │  │
│  │     ├─ Efficiency = Speedup / N × 100%                   │  │
│  │     ├─ Ideal speedup = N                                 │  │
│  │     └─ Scalability = Speedup / Ideal                     │  │
│  │                                                          │  │
│  │  3. Export data:                                         │  │
│  │     ├─ JSON: structured results                          │  │
│  │     └─ CSV: tabular format                               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Weak Scaling Analysis (weak_scaling.py)                 │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │                                                          │  │
│  │  Algorithm:                                              │  │
│  │  1. For each processor count [1, 2, 4, 8]:              │  │
│  │     ├─ Scale problem size proportionally                 │  │
│  │     │  (constant work per processor)                     │  │
│  │     ├─ Run matrix operations                             │  │
│  │     └─ Measure execution time                            │  │
│  │                                                          │  │
│  │  2. Calculate metrics:                                   │  │
│  │     ├─ Efficiency = T(1) / T(N) × 100%                   │  │
│  │     └─ Ideal: constant time                              │  │
│  │                                                          │  │
│  │  3. Export data:                                         │  │
│  │     ├─ JSON: structured results                          │  │
│  │     └─ CSV: tabular format                               │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 5. Visualization Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│              VISUALIZATION SYSTEM                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Visualization Engine (visualize.py)                     │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │                                                          │  │
│  │  Plot Functions:                                         │  │
│  │                                                          │  │
│  │  1. plot_strong_scaling()                                │  │
│  │     ├─ Subplot 1: Execution Time vs Processors           │  │
│  │     │  └─ Line plot with markers                         │  │
│  │     ├─ Subplot 2: Speedup (Actual vs Ideal)              │  │
│  │     │  └─ Dual line plot                                 │  │
│  │     ├─ Subplot 3: Parallel Efficiency                    │  │
│  │     │  └─ Bar chart                                      │  │
│  │     └─ Subplot 4: Summary Table                          │  │
│  │        └─ Pandas table rendered                          │  │
│  │                                                          │  │
│  │  2. plot_communication_bottleneck()                      │  │
│  │     ├─ Subplot 1: Computation vs Communication           │  │
│  │     │  └─ Stacked bar chart                              │  │
│  │     └─ Subplot 2: Communication Overhead %               │  │
│  │        └─ Line plot with annotations                     │  │
│  │                                                          │  │
│  │  3. plot_resource_monitoring()                           │  │
│  │     ├─ Subplot 1: CPU Usage over time                    │  │
│  │     ├─ Subplot 2: Memory Usage over time                 │  │
│  │     ├─ Subplot 3: Network I/O                            │  │
│  │     └─ Subplot 4: Disk I/O                               │  │
│  │                                                          │  │
│  │  Styling:                                                │  │
│  │  ├─ Seaborn theme: "whitegrid"                           │  │
│  │  ├─ Color palette: "husl" (distinct colors)              │  │
│  │  ├─ DPI: 300 (high resolution)                           │  │
│  │  ├─ Figure size: (16, 12) inches                         │  │
│  │  └─ Font: sans-serif, size 10-14                         │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔌 Communication Patterns

### MPI Communication Topology

```
┌─────────────────────────────────────────────────────────────────┐
│                 MPI COMMUNICATION PATTERNS                       │
└─────────────────────────────────────────────────────────────────┘

1. Broadcast Pattern (One-to-All)
──────────────────────────────────────────────────────────────────
   Used for: Distributing Matrix B to all processes

                     ┌──────────┐
                     │ Rank 0   │
                     │ (Root)   │
                     │ Matrix B │
                     └────┬─────┘
                          │
          ┌───────────────┼───────────────┐
          │               │               │
          ▼               ▼               ▼
   ┌──────────┐    ┌──────────┐    ┌──────────┐
   │ Rank 1   │    │ Rank 2   │    │ Rank N   │
   │ Matrix B │    │ Matrix B │    │ Matrix B │
   └──────────┘    └──────────┘    └──────────┘

   MPI Call: MPI_Bcast(&matrix_B, size, MPI_DOUBLE, 0, comm)
   Complexity: O(log P) where P = number of processes


2. Scatter Pattern (One-to-Many, Split Data)
──────────────────────────────────────────────────────────────────
   Used for: Distributing Matrix A rows to processes

                     ┌──────────────┐
                     │   Rank 0     │
                     │   (Root)     │
                     │  Matrix A    │
                     │  [Row 0-4095]│
                     └───────┬──────┘
                             │
          ┌──────────────────┼──────────────────┐
          │                  │                  │
   Rows 0-1023        Rows 1024-2047     Rows 2048-3071
          │                  │                  │
          ▼                  ▼                  ▼
   ┌──────────┐       ┌──────────┐       ┌──────────┐
   │ Rank 0   │       │ Rank 1   │       │ Rank 2   │
   │ Rows 0-  │       │ Rows 1024│       │ Rows 2048│
   │   1023   │       │   -2047  │       │   -3071  │
   └──────────┘       └──────────┘       └──────────┘

   MPI Call: MPI_Scatter(matrix_A, rows_per_proc, ..., comm)
   Complexity: O(N) where N = data size


3. Gather Pattern (Many-to-One, Collect Data)
──────────────────────────────────────────────────────────────────
   Used for: Collecting partial Matrix C results

   ┌──────────┐       ┌──────────┐       ┌──────────┐
   │ Rank 0   │       │ Rank 1   │       │ Rank 2   │
   │ Partial C│       │ Partial C│       │ Partial C│
   │ Rows 0-  │       │ Rows 1024│       │ Rows 2048│
   │   1023   │       │   -2047  │       │   -3071  │
   └─────┬────┘       └─────┬────┘       └─────┬────┘
         │                  │                  │
         └──────────────────┼──────────────────┘
                            │
                            ▼
                     ┌──────────────┐
                     │   Rank 0     │
                     │   (Root)     │
                     │  Matrix C    │
                     │  [All Rows]  │
                     └──────────────┘

   MPI Call: MPI_Gather(partial_C, ..., full_C, ..., 0, comm)
   Complexity: O(N) where N = data size


4. Reduce Pattern (Many-to-One, with Operation)
──────────────────────────────────────────────────────────────────
   Used for: Calculating global statistics (optional)

   ┌──────────┐       ┌──────────┐       ┌──────────┐
   │ Rank 0   │       │ Rank 1   │       │ Rank 2   │
   │ Local    │       │ Local    │       │ Local    │
   │ Time: 2s │       │ Time: 3s │       │ Time: 2s │
   └─────┬────┘       └─────┬────┘       └─────┬────┘
         │                  │                  │
         └──────────────────┼──────────────────┘
                            │ MPI_Reduce (MPI_MAX)
                            ▼
                     ┌──────────────┐
                     │   Rank 0     │
                     │   Max Time   │
                     │     3s       │
                     └──────────────┘

   MPI Call: MPI_Reduce(&local_time, &max_time, 1, 
                        MPI_DOUBLE, MPI_MAX, 0, comm)
   Complexity: O(log P)
```

---

## 🎯 Performance Optimization Strategy

```
┌─────────────────────────────────────────────────────────────────┐
│              OPTIMIZATION LAYERS                                 │
└─────────────────────────────────────────────────────────────────┘

1. Algorithmic Level
──────────────────────────────────────────────────────────────────
   • Row-wise matrix decomposition
     └─ Minimizes communication (only broadcast B once)
   • Block-based computation
     └─ Cache-friendly memory access
   • Strassen algorithm potential
     └─ O(n^2.807) vs O(n^3)

2. Parallelization Level
──────────────────────────────────────────────────────────────────
   • MPI: Distributed memory parallelism
     ├─ Scales across multiple nodes
     └─ Minimize communication overhead
   • OpenMP: Shared memory parallelism
     ├─ Efficient within single node
     └─ Dynamic load balancing

3. Data Structure Level
──────────────────────────────────────────────────────────────────
   • Contiguous memory allocation
     └─ Cache line optimization
   • Array padding
     └─ Avoid false sharing
   • Structure of Arrays (SoA) vs Array of Structures (AoS)
     └─ Vectorization-friendly layout

4. I/O Optimization Level
──────────────────────────────────────────────────────────────────
   • HDF5 chunking
     └─ Efficient partial reads/writes
   • Compression
     └─ Reduce storage and I/O time
   • Parallel I/O
     └─ MPI-IO for concurrent access

5. System Level
──────────────────────────────────────────────────────────────────
   • CPU affinity binding
     └─ Prevent process migration
   • NUMA-aware allocation
     └─ Local memory access
   • Network optimization
     └─ InfiniBand/high-speed interconnect
```

---

## 📊 Scalability Model

### Amdahl's Law Analysis

```
Speedup(P) = 1 / (S + (1-S)/P)

Where:
  P = Number of processors
  S = Serial fraction (non-parallelizable)

For our system:
  S ≈ 0.10 (10% communication/serial overhead)
  
Maximum theoretical speedup:
  Speedup_max = 1 / S = 1 / 0.10 = 10×

Actual measurements (4096×4096):
  P=1: T=7.19s,  Speedup=1.0×  (baseline)
  P=2: T=13.87s, Speedup=0.52× (overhead dominates for small test)
  P=4: T=6.54s,  Speedup=1.10× (starts to show benefit)
```

### Gustafson's Law (Weak Scaling)

```
Speedup(P) = S + P(1-S)

Better model for large-scale systems where:
  - Problem size grows with processors
  - Serial fraction decreases with larger problems

Expected for larger matrix sizes (8192×8192, etc.):
  - Better efficiency
  - Near-linear scaling
```

---

## 🔐 Design Patterns Used

1. **Master-Worker Pattern**
   - Root process distributes work
   - Worker processes compute independently
   - Root collects and aggregates results

2. **Pipeline Pattern**
   - Data flows through processing stages
   - Generate → Distribute → Compute → Gather → Store

3. **Façade Pattern**
   - Simplified interface for complex MPI operations
   - Hide implementation details from user

4. **Strategy Pattern**
   - Different algorithms (C++/Python implementations)
   - Switchable at runtime

5. **Observer Pattern**
   - Resource monitoring observes system state
   - Logs changes continuously

---

## 🌐 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    DEPLOYMENT SCENARIOS                          │
└─────────────────────────────────────────────────────────────────┘

1. Single Node (Shared Memory)
──────────────────────────────────────────────────────────────────
   [Node]
   ├─ Multiple MPI processes
   ├─ OpenMP threads per process
   └─ Shared memory communication
   
   Command: mpirun -np 4 --bind-to core ./program

2. Multi-Node Cluster (Distributed Memory)
──────────────────────────────────────────────────────────────────
   [Node 0] ← Network → [Node 1] ← Network → [Node N]
   ├─ Processes       ├─ Processes       ├─ Processes
   └─ Threads         └─ Threads         └─ Threads
   
   Command: mpirun -np 16 --hostfile nodes.txt ./program

3. Cloud Deployment (Elastic Scaling)
──────────────────────────────────────────────────────────────────
   AWS/Azure/GCP:
   ├─ Auto-scaling groups
   ├─ Container orchestration (Kubernetes)
   ├─ Elastic File System (distributed storage)
   └─ Load balancing
```

---

**Document Version:** 1.0  
**Last Updated:** December 3, 2025  
**Architecture Status:** ✅ Implemented and Tested
